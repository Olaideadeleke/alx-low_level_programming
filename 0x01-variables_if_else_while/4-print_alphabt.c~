#include <stdio.h>
/**
 *main - Entry Point
 *
 *Return: Always 0 (Succes)
 *
 */
int main(void)
{
	char c;

	for (c = 'a'; c <= 'z'; c++)
	{
		putchar(c);
		if (c == 'q' || c == 'e')
		{
			return (0);
		}
	}
	putchar('\n');
	return (0);
}
