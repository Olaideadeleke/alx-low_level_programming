#include "main.h"
#include <stdio.h>
/**
 *add - add two numbers
 *@a: first integer
 *@b: second integer
 *Return: Always result (Success)
 */
int add(int a, int b)
{
	int result;

	result = a + b;
	return (result);
}
